import React from 'react';
import {View, Text} from 'react-native';
import AppStackScreen from './navigation/MainNavigation';

const App = props => {
  return <AppStackScreen />;
};

export default App;
